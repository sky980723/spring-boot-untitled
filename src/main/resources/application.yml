server:
  port: 8099

spring:
  application:
    name: untitled-one
  # 数据库配置
  datasource:
    url: jdbc:mysql://localhost:3306/untitled?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=GMT%2B8
    username: root
    password: 123456
    driver-class-name: com.mysql.jdbc.Driver
    type: com.alibaba.druid.pool.DruidDataSource
  # Redis配置
  redis:
    port: 6379
    host: 127.0.0.1
    password:
    database: 0
    timeout: 10000
